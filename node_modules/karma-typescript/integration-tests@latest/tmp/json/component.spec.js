"use strict";
var component_1 = require("./component");
describe("JsonComponent", function () {
    it("should require a json file without crashing the bundler", function () {
        var jsonComponent = new component_1.JsonComponent();
        expect(jsonComponent.run()).toEqual([1, 2, 3, "a", "b", "c"]);
    });
});
