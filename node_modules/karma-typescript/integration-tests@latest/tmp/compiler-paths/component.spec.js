"use strict";
var component_1 = require("compilerpaths/component");
describe("CompilerPathComponent", function () {
    it("should use paths option in tsconfig without crashing the bundler", function () {
        var compilerPathComponent = new component_1.CompilerPathComponent();
        expect(compilerPathComponent.run()).toEqual("I didn't break the bundler :)");
    });
});
