"use strict";
var component_1 = require("./component");
describe("ExportsComponent", function () {
    it("should use default exported module", function () {
        var exportsComponent = new component_1.ExportsComponent();
        expect(exportsComponent.format(new Date(2014, 3, 23))).toEqual("2014-04-23");
    });
    it("should extend modules without crashing on non-extensible objects", function () {
        var exportsComponent = new component_1.ExportsComponent();
        expect(exportsComponent.hasMap()).toBeTruthy();
    });
});
