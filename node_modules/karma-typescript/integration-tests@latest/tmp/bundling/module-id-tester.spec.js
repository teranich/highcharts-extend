"use strict";
describe("ModuleIdTester", function () {
    it("should have a module.id equal to __filename", function () {
        expect(module.uri).toEqual(__filename);
        expect(module.id).toEqual("src/bundling/module-id-tester.spec.ts");
    });
});
