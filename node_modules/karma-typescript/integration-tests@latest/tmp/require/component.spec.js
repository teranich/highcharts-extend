"use strict";
var r = require("./component");
describe("RequireComponent", function () {
    it("should be able to require dummy content and real modules", function () {
        var requireComponent = new r.RequireComponent();
        expect(requireComponent.run()).toBeTruthy();
    });
});
