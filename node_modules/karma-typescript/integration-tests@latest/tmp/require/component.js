"use strict";
require("../../assets/style/require.css");
var RequireComponent = (function () {
    function RequireComponent() {
    }
    RequireComponent.prototype.run = function () {
        var util = require("util");
        var req = require("./dependency");
        var dep = new req.DependencyComponent();
        return util !== undefined && dep.dependOnMe() === "I was required!";
    };
    return RequireComponent;
}());
exports.RequireComponent = RequireComponent;
