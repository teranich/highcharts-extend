"use strict";
var events = require("events");
var core_module_tester_1 = require("./core-module-tester");
describe("CoreModuleComponent", function () {
    var tester = new core_module_tester_1.CoreModuleTester();
    it("should use core module assert", function () {
        expect(tester.testAssert()).toEqual(jasmine.any(Function));
    });
    it("should use core module buffer", function () {
        expect(tester.testBuffer()).toEqual(new Buffer("string"));
    });
    it("should use core module console", function () {
        expect(tester.testConsole()).toEqual(jasmine.any(Function));
    });
    it("should use core module constants", function () {
        expect(tester.testConstants()).toEqual(2);
    });
    it("should use core module crypto", function () {
        expect(tester.testCrypto()).toEqual("b45cffe084dd3d20d928bee85e7b0f21");
    });
    it("should use core module domain", function () {
        expect(tester.testDomain()).toEqual(jasmine.any(events.EventEmitter));
    });
    it("should use core module events", function () {
        expect(tester.testEvents()).toEqual(jasmine.any(events.EventEmitter));
    });
    it("should use core module http", function () {
        expect(tester.testHttp()).not.toBeUndefined();
    });
    it("should use core module os", function () {
        expect(tester.testOs()).toEqual("browser");
    });
    it("should use core module util", function () {
        expect(tester.testUtil()).toBeTruthy();
    });
});
