import { IAppendValueParams } from "./IAppendValueParams";
export declare class KeyValueStorage<U> {
    private _storage;
    private forceAppend(params);
    add(...params: IAppendValueParams<U>[]): void;
    remove(key: string): void;
    get(key: string): U;
    check(key: string): boolean;
}
