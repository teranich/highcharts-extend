"use strict";
var angularInputValidator_1 = require("./angularInputValidator");
describe('case for input validator', function () {
    it('test valid object, return value must be null', function () {
        var result = angularInputValidator_1.AngularInputValidator.checkRequireAttributes({
            requireAttributes: ['a', 'b'],
            directiveContext: { a: 'a', b: 'b' }
        });
        expect(result).toBeNull();
    });
    it('test invalid object, return value must be error that value b is missing', function () {
        var result = angularInputValidator_1.AngularInputValidator.checkRequireAttributes({
            requireAttributes: ['a', 'b'],
            directiveContext: { a: 'a' }
        });
        expect(result).not.toBeNull();
    });
    //TODO: write more tests
});
//# sourceMappingURL=angularInputValidator.spec.js.map