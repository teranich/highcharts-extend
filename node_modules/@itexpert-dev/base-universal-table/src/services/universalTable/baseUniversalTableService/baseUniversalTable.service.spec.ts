import {BaseUniversalTableService} from './baseUniversalTable.service'
import {UniversalTableConfigurator} from "../baseUniversalTableConfigurator/baseUniversalTableConfigurator";
import any = jasmine.any;

describe('test for base universal table service', ()=>{
    it('method for test create new instance', ()=>{
        expect(()=>{
            let instance = new BaseUniversalTableService(new UniversalTableConfigurator());
        }).not.toThrow()
    })
})